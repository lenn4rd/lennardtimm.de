<% if build? %>
  <%= javascript_include_tag 'sp' %>

  <script type="text/javascript" async=1>
    ;(function(p,l,o,w,i,n,g){if(!p[i]){p.GlobalSnowplowNamespace=p.GlobalSnowplowNamespace||[];
    p.GlobalSnowplowNamespace.push(i);p[i]=function(){(p[i].q=p[i].q||[]).push(arguments)
    };p[i].q=p[i].q||[];n=l.createElement(o);g=l.getElementsByTagName(o)[0];n.async=1;
    n.src=w;g.parentNode.insertBefore(n,g)}}(window,document,"script","/javascripts/sp.js","snowplow"));

    window.snowplow('newTracker', 'cf', '<%= ENV['COLLECTOR_HOST'] %>', {
      appId: 'lennardtimm.de',
      cookieDomain: 'lennardtimm.de',
      forceSecureTracker: true,
      respectDoNotTrack: true
    });

    window.snowplow('enableActivityTracking', 30, 10);
    window.snowplow('trackPageView');
  </script>
<% end %>
